name: CD

on:
    push:
        branches:
            - develop

jobs:
    deploy:
        name: Deploy to Stage
        runs-on: ubuntu-latest
        steps:
            - run: curl "https://api.nsm.mosaiq.dev/deploy/terrazzo-staging/${{ secrets.NSM_KEY_STAGE }}"
