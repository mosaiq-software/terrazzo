# Terrazzo Common
This package contains shared utilities and types used across the Terrazzo monorepo.

All functions must be exported up to the main `index.ts` file to be accessible by other packages.